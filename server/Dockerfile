# שלב 1: שימוש בתמונה של Node.js
FROM node:20

# שלב 2: יצירת תיקיית עבודה בתוך הקונטיינר
WORKDIR /app

# שלב 3: העתקת קבצי הפרויקט לקונטיינר
COPY package*.json ./

# שלב 4: התקנת תלויות
RUN npm install

# שלב 5: העתקת שאר קבצי הקוד
COPY . .

# שלב 6: פתיחת פורט (אם את מריצה על פורט 3000 לדוגמה)
EXPOSE 3001

# שלב 7: הרצת האפליקציה
CMD ["npm", "start"]
